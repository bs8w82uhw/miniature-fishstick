name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Placeholder for linting
        run: |
          echo "Linting step placeholder"
          echo "Add your linting commands here based on your project stack"

  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Placeholder for tests
        run: |
          echo "Testing step placeholder"
          echo "Add your test commands here based on your project stack"

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Placeholder for build
        run: |
          echo "Build step placeholder"
          echo "Add your build commands here based on your project stack"
