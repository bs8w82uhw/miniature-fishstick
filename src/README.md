# Source Code

This directory will contain the project source code.

## Structure

Add subdirectories as needed for your project:
- `components/` - Reusable components
- `utils/` - Utility functions
- `services/` - Business logic and services
- `models/` - Data models
- `tests/` - Test files

Update this file as the project structure evolves.
